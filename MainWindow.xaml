<Window x:Class="Surprise_Attack_test.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Surprise_Attack_test"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"      
        mc:Ignorable="d" 
        Title="Suprise Attack - Simulator" Height="811" Width="1390"
        Background="#E8ECEF">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#E2E8F0"/>
            <Setter Property="Foreground" Value="#2D3748"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" BorderBrush="#CBD5E0" BorderThickness="1">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#CBD5E0"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="330"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Margin="15" CornerRadius="10" Background="Black" ClipToBounds="True">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" ShadowDepth="5" Opacity="0.3"/>
            </Border.Effect>
            <Image x:Name="MapImage"
                   Stretch="Uniform"           
                   RenderOptions.BitmapScalingMode="NearestNeighbor"
                   MouseLeftButtonDown="MapImage_MouseLeftButtonDown"/>
        </Border>

        <Border Grid.Column="1" Background="#F7FAFC" BorderBrush="#E2E8F0" BorderThickness="1,0,0,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="15">
                <StackPanel>

                    <TextBlock Text="Control Panel" FontSize="24" FontWeight="Bold" Foreground="#2D3748" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                    <Border Background="White" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.1"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock Text="Map Management" FontWeight="Bold" Foreground="#4A5568" Margin="0,0,0,10"/>
                            <UniformGrid Columns="3" HorizontalAlignment="Stretch">
                                <Button Name="SaveMap_Button" Content="Save" Margin="0,0,5,0" Height="30" Click="SaveMap_Click"/>
                                <Button Name="LoadMap_Button" Content="Load" Margin="5,0,5,0" Height="30" Click="LoadMap_Click"/>
                                <Button Name="ClearMap_Button" Content="Clear" Margin="5,0,0,0" Height="30" Click="ClearMap_Click" Foreground="#E53E3E"/>
                            </UniformGrid>
                        </StackPanel>
                    </Border>

                    <Border Background="White" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.1"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock Text="Environment Tools" FontWeight="Bold" Foreground="#4A5568" Margin="0,0,0,10"/>

                            <UniformGrid Columns="2" Rows="3" HorizontalAlignment="Stretch">
                                <Button Name="StartPos_Button" Content="Set Start" Margin="0,0,5,10" Height="35" Click="StartPos_Click" Background="#C6F6D5"/>
                                <Button Name="TargetPos_Button" Content="Set Target" Margin="5,0,0,10" Height="35" Click="TargetPos_Click" Background="#FED7D7"/>

                                <Button Name="AddMountain_Button" Content="Add Mountain" Margin="0,0,5,10" Height="35" Click="AddMountain_Click"/>
                                <Button Name="AddCamera_Button" Content="Add Camera" Margin="5,0,0,10" Height="35" Click="AddCamera_Click"/>

                                <Button Name="DeleteCamera_Button" Content="Delete Camera" Margin="0,0,5,0" Height="35" Click="DeleteCamera_Click"/>
                            </UniformGrid>

                            <CheckBox x:Name="chkShowRestricted" Content="Show Restricted Areas" FontSize="14" FontWeight="SemiBold" Foreground="#E53E3E" Margin="0,15,0,0" Checked="RestrictedArea_Checked" Unchecked="RestrictedArea_Unchecked"/>
                        </StackPanel>
                    </Border>

                    <Border Background="White" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.1"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock Text="Simulation Settings" FontWeight="Bold" Foreground="#4A5568" Margin="0,0,0,10"/>

                            <Grid Margin="0,5,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Total Generations:" VerticalAlignment="Center" FontSize="14" FontWeight="SemiBold" Foreground="#4A5568" Margin="0,0,10,0"/>
                                <TextBox x:Name="GenCountBox" Grid.Column="1" Text="20" Height="28" FontSize="14" FontWeight="Bold" TextAlignment="Center" VerticalContentAlignment="Center"/>
                            </Grid>

                            <Label Name="GenCount_Label" Content="Current Generation: 0" FontSize="14" Foreground="#718096" HorizontalAlignment="Left" Padding="0"/>
                        </StackPanel>
                    </Border>

                    <Border Background="White" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.1"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock Text="Execution" FontWeight="Bold" Foreground="#4A5568" Margin="0,0,0,10"/>

                            <Button Name="StartSimulation_Button" Content="▶ Start Simulation" Height="40" Background="#3182CE" Foreground="White" FontSize="15" Margin="0,0,0,10" IsEnabled="False" Click="StartSimulation_Click"/>
                            <Button Name="FastStartSimulation_Button" Content="⏩ Fast Start (No Delay)" Height="35" Background="#2B6CB0" Foreground="White" Margin="0,0,0,10" IsEnabled="False" Click="FastStartSimulation_Click"/>
                            <Button Name="BestAnt_Button" Content="⭐ Show Best Ant" Height="35" Background="#D69E2E" Foreground="White" IsEnabled="False" Click="BestAnt_Click"/>
                        </StackPanel>
                    </Border>

                    <Border Background="White" CornerRadius="8" Padding="15" MinHeight="250">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.1"/>
                        </Border.Effect>
                        <Grid>
                            <lvc:CartesianChart x:Name="ConvergenceGraphUI" 
                    Series="{Binding SeriesCollection}" 
                    Zoom="X" 
                    Pan="X" 
                    Background="Transparent">
                                <lvc:CartesianChart.AxisX>
                                    <lvc:Axis Title="Generation" MinValue="1">
                                        <lvc:Axis.Separator>
                                            <lvc:Separator Step="10" />
                                        </lvc:Axis.Separator>
                                    </lvc:Axis>
                                </lvc:CartesianChart.AxisX>
                                <lvc:CartesianChart.AxisY>
                                    <lvc:Axis Title="Best Distance" LabelFormatter="{Binding YFormatter}"/>
                                </lvc:CartesianChart.AxisY>
                            </lvc:CartesianChart>
                            <ContentControl x:Name="ChartContainer" />
                        </Grid>
                    </Border>

                </StackPanel>
            </ScrollViewer>
        </Border>

        <Border Name="MountainInputOverlay" Background="White" Width="280" Height="220" CornerRadius="12" BorderBrush="#E2E8F0" BorderThickness="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="25" ShadowDepth="5" Opacity="0.4"/>
            </Border.Effect>

            <StackPanel Margin="20" VerticalAlignment="Center">
                <TextBlock Text="Mountain Settings" FontSize="18" FontWeight="Bold" Foreground="#2D3748" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <Grid Margin="0,5,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Grid.Column="0" Content="Radius:" FontWeight="SemiBold"/>
                    <TextBox Grid.Column="1" x:Name="txtOverlayRadius" Height="28" VerticalContentAlignment="Center" TextAlignment="Center" Text="6"/>
                </Grid>

                <Grid Margin="0,5,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Grid.Column="0" Content="Height:" FontWeight="SemiBold"/>
                    <TextBox Grid.Column="1" x:Name="txtOverlayHeight" Height="28" VerticalContentAlignment="Center" TextAlignment="Center" Text="220"/>
                </Grid>

                <UniformGrid Columns="2" HorizontalAlignment="Stretch">
                    <Button Content="Cancel" Height="32" Margin="0,0,5,0" Click="OverlayCancel_Click"/>
                    <Button Content="Confirm" Height="32" Margin="5,0,0,0" Background="#48BB78" Foreground="White" Click="OverlayConfirm_Click"/>
                </UniformGrid>
            </StackPanel>
        </Border>
    </Grid>
</Window>